services:
  - type: web
    name: alphabot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.11"
      - key: FIREBASE_SERVICE_ACCOUNT
        sync: false
